apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "token-exchange-demo.fullname" . }}-test-connection"
  labels:
    {{- include "token-exchange-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
  - name: wget
    image: busybox
    command: ['wget']
    args:  ['{{ include "token-exchange-demo.fullname" . }}:{{ .Values.service.port }}/?healthz=true']